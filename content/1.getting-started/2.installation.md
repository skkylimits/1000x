---
title: Installation
description: Originele template-installatie — bewaard als demo, niet de 1000x-installatie.
navigation:
  icon: i-lucide-download
---

> Voor de échte 1000x-installatie zie [`/installation`](/installation). Deze pagina is de oorspronkelijke template-quickstart van de docs-template, bewaard voor markdown-render-coverage.

## Quick Start (originele template)

You can start a fresh new project with:

```bash [Terminal]
npx nuxi init -t github:nuxt-ui-templates/docs
```

or create a new repository from GitHub:

1. Open <https://github.com/nuxt-ui-templates/docs>
2. Click on `Use this template` button
3. Enter repository name and click on `Create repository from template` button
4. Clone your new repository
5. Install dependencies with your favorite package manager
6. Start development server

That's it! You can now start writing your documentation in the [`content/`](https://content.nuxt.com/usage/content-directory) directory 🚀
