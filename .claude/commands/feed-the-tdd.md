---
description: Feed een code-review lesson naar de juiste TDD-doc via de compounding-learning routing heuristiek
---

User wil een lesson-learned routeren naar de juiste TDD-doc.

Invoke de `feed-the-tdd` skill (in `.claude/skills/feed-the-tdd/SKILL.md`) en pas de routing toe.

Lesson om te routeren:

$ARGUMENTS

Als $ARGUMENTS leeg is, vraag de user eerst om de lesson te beschrijven (wat de fout was / welke regel ze willen encoden).